# Copyright (C) 1995-1998, Index Data I/S 
# All rights reserved.
# Sebastian Hammer, Adam Dickmeiss
# $Id: Makefile.in,v 1.1 1998-08-21 14:13:38 adam Exp $

SHELL=/bin/sh

CC=@CC@
CPP=@CPP@
RANLIB=@RANLIB@
CDEFS=@DEFS@ @ODEFS@
ELIBS=@LIBS@

LIBDIR=../lib

#LIBMOSI=../../xtimosi/src/libmosi.a $(LIBDIR)/librfc.a

SHELL=/bin/sh
INCLUDE=-I../include -I. -I../../xtimosi/src
DEFS=$(INCLUDE) $(CDEFS)
LIB=$(LIBDIR)/libserver.a 
LIBS=$(LIBDIR)/libserver.a $(LIBDIR)/libasn.a $(LIBDIR)/libodr.a \
$(LIBDIR)/libcomstack.a $(LIBMOSI) $(LIBDIR)/libutil.a
PROG=ztest
PROGO=ztest.o read-grs.o
RANLIB=ranlib

all: $(PROG)

$(PROG): $(LIB) $(PROGO) $(LIBS)
	$(CC) $(CFLAGS) -o $(PROG) $(PROGO) $(LIBS) $(ELIBS)

alll:

.c.o:
	$(CC) -c $(DEFS) $(CFLAGS) $<

clean:
	rm -f *.[oa] core mon.out gmon.out errlist $(PROG)

depend:
	sed '/^#Depend/q' <Makefile >Makefile.tmp
	$(CPP) $(DEFS) -M *.c >>Makefile.tmp
	mv -f Makefile.tmp Makefile

#Depend --- DOT NOT DELETE THIS LINE
